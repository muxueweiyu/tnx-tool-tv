#!/usr/bin/env node

/**
 * tnx-cli entrypoint
 * 
 * 按照 00-global.md 规定，此文件仅用于拦截初始化配置和路由导向。
 * 绝对不应包含具体某个命令的业务逻辑！！！
 */

console.log("Welcome to tnx-cli!");

// FUTURE: Initialize Commander/Yargs here and import commands from ./commands.
// Example:
// import { initCommands } from './commands/init';
// initCommands();
